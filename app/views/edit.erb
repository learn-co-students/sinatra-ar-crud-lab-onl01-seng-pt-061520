<!DOCTYPE html>
<html>
<head>
	<title>Edit <%= @article.title %></title>
</head>
<body>
	<form method="POST" action="/articles/<%= @article.id %>">
		<input type="hidden" name="_method" value="PATCH">
		<p>Title:</p>
		<textarea rows="1" cols="80" name="title" id="title"><%= @article.title %></textarea>
		<p>Content:</p>
		<textarea rows="10" cols="80" name="content" id="content"><%= @article.content %></textarea>
		<input type="submit" id="submit">
	</form>
</body>
</html>